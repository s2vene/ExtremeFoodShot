# ExtremeFoodShot 기술 테스트

이 브랜치는 제품 UI가 아니라 실제 iPhone에서 촬영 가능성을 검증하기 위한 계측형 프로토타입이다.

## 필요한 환경

- iOS 17 이상을 실행하는 실제 iPhone
- 후면 카메라와 플래시/토치를 사용할 수 있는 기기
- 음식 또는 질감과 하이라이트가 분명한 피사체
- 동일 조건 비교를 위한 고정된 실내 조명

시뮬레이터에서는 카메라, 토치, Device Motion 결과를 검증할 수 없다.

## 기본 실험

1. Xcode에서 `ExtremeFoodShot` 스킴과 연결된 iPhone을 선택한다.
2. Signing & Capabilities에서 본인의 Development Team을 선택한다.
3. 앱을 실행하고 카메라, 동작 및 사진 추가 권한을 허용한다.
4. 피사체 위에서 폰 뒷면이 음식과 평행하도록 잡는다.
5. `토치 연속광`을 선택하고 `테스트 시작`을 누른다.
6. 폰을 음식에 가까워졌다 멀어지는 방향으로 3~5회 움직인다.
7. `촬영 완료`를 누르고 후보별 추천 점수와 센서 값을 비교한다.
8. 같은 동작을 `사진 플래시`와 `조명 없음`으로 반복한다.

## 비교 기록

각 조명 모드마다 최소 10회 반복하고 아래 항목을 기록한다.

- 후보가 한 장 이상 생성된 횟수
- 음식이 식별 가능한 후보가 생성된 횟수
- 방사형 블러가 원하는 방향으로 나온 횟수
- 회전 블러 또는 구도 이탈 횟수
- 과노출/저노출 횟수
- 가장 좋은 후보의 추천 순위

## 튜닝 순서

1. 기본값: 광축 가속 `0.40g`, 회전 허용치 `3.4 rad/s`
2. 촬영이 거의 발생하지 않으면 광축 가속을 `0.1g`씩 낮춘다.
3. 너무 자주 촬영되면 광축 가속을 `0.1g`씩 높인다.
4. 비틀린 사진이 많으면 회전 허용치를 낮춘다.
5. 의도한 역동성이 부족하면 회전 허용치는 유지하고 움직임 크기만 늘린다.

## 카메라 기능 비교

튜닝 화면에서 한 번에 한 항목만 변경하고 같은 동작을 최소 5회 반복한다.

1. 렌즈: 초광각, 광각, 망원의 구도 이탈과 원근감을 비교한다.
2. 노출시간: `1/250초`, `1/60초`, `1/15초`의 정지감과 블러를 비교한다.
3. 초점: 연속 자동과 현재 위치 고정에서 초점 이동 실패율을 비교한다.
4. 화이트밸런스: 자동과 고정에서 후보 사이 색상 변화를 비교한다.
5. 촬영 처리: 속도/균형/화질 우선, Zero Shutter Lag, 왜곡 보정을 각각 비교한다.
6. 토치: 10% 단위로 밝기를 바꾸며 반사광과 과노출을 기록한다.
7. 프레임 구간: 트리거 이전/이후 범위와 트리거당 후보 수를 조절한다.

RAW, ProRAW, Depth/LiDAR와 일부 처리 기능은 기기와 활성 렌즈에 따라 지원 여부가
달라지므로 튜닝 화면의 `기기 지원 확인`에서 먼저 확인한다.

## 현재 범위

- 100Hz Device Motion 수집
- 광축 가속과 회전량 기반 방향 전환 감지
- 토치, 사진 플래시, 조명 없음 비교
- 움직임 이벤트마다 사진 후보 자동 촬영
- 프레임 밝기와 윤곽 에너지 계측
- 후보 추천 정렬 및 선택 사진 저장

후속 실험에서는 수동 노출 프리셋, 영상 프레임 링 버퍼, optical flow 기반 방사형 움직임 점수를 추가한다.
